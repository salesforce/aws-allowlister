name: Publish

on:
  release:
    types: [ published ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Bump version
        run: sh ${GITHUB_WORKSPACE}/utils/bump-version.sh
